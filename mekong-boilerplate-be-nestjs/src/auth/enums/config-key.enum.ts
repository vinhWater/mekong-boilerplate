/**
 * Enum representing configuration keys grouped by type
 * Provider keys: GEARMENT, CUSTOMCAT
 * Future: Notification and Chrome keys can be added
 */
export enum ConfigKey {
    // Provider keys
    GEARMENT = 'Gearment',
    CUSTOMCAT = 'Customcat',

    // Future: Add Notification keys here
    // Future: Add Chrome keys here

    // AI_Assistant keys 
    GEMINI = 'Gemini',
}
